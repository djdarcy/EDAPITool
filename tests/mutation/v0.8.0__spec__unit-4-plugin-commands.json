{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_plugin_commands.py", "tests/test_plugins_verb.py", "tests/test_plugin_flags.py"],
  "mutations": [
    {"id": "M01", "file": "APITool/cli.py", "original": "    tail = list(getattr(args, \"tail\", None) or [])", "mutated": "    tail = list(getattr(args, \"tail\", None) and [])", "note": "and/or swap on the tail normalization: a non-empty tail becomes []; None raises TypeError"},
    {"id": "M02", "file": "APITool/cli.py", "original": "    if name == \"help\":\n        parser = getattr(args, \"_plugins_parser\", None)", "mutated": "    if name != \"help\":\n        parser = getattr(args, \"_plugins_parser\", None)", "note": "comparison flip: every name but help enters the help branch"},
    {"id": "M03", "file": "APITool/cli.py", "original": "        if parser is not None:\n            parser.print_help()\n        return 0", "mutated": "        if parser is None:\n            parser.print_help()\n        return 0", "note": "None flip on the parser guard: help is skipped when a parser is present"},
    {"id": "M04", "file": "APITool/cli.py", "original": "    if name == \"describe\":\n        if not tail:\n            print(\"Error: `plugins describe` needs a plugin name.\")", "mutated": "    if name == \"describe\":\n        if tail:\n            print(\"Error: `plugins describe` needs a plugin name.\")", "note": "inverted guard: the error fires when a name WAS supplied; bare describe raises IndexError"},
    {"id": "M05", "file": "APITool/cli.py", "original": "            print(\"Error: `plugins describe` needs a plugin name.\")\n            print(f\"       Found: {', '.join(sorted(f.name for f in found))}\")\n            return 2", "mutated": "            print(\"Error: `plugins describe` needs a plugin name.\")\n            print(f\"       Found: {', '.join(sorted(f.name for f in found))}\")\n            return 0", "note": "wrong literal: a bare describe exits 0"},
    {"id": "M06", "file": "APITool/cli.py", "original": "        return _describe_plugin(tail[0], found, targets)", "mutated": "        return _describe_plugin(found, tail[0], targets)", "note": "swapped arguments to _describe_plugin"},
    {"id": "M07", "file": "APITool/cli.py", "original": "    if name is not None and name not in RESERVED:\n        return _run_plugin_command(name, tail, found, targets)", "mutated": "    if name is not None or name not in RESERVED:\n        return _run_plugin_command(name, tail, found, targets)", "note": "and/or swap: a bare `plugins` dispatches _run_plugin_command(None, ...)"},
    {"id": "M08", "file": "APITool/cli.py", "original": "    ask = getattr(module, \"commands\", None)\n    if not callable(ask):\n        return {}", "mutated": "    ask = getattr(module, \"commands\", None)\n    if callable(ask):\n        return {}", "note": "boolean flip: plugins that define commands() get {}; plugins that do not call None()"},
    {"id": "M09", "file": "APITool/cli.py", "original": "    offered = ask()\n    if not isinstance(offered, Mapping):\n        raise TypeError(f\"commands() returned {type(offered).__name__}, not a mapping\")", "mutated": "    offered = ask()\n    if isinstance(offered, Mapping):\n        raise TypeError(f\"commands() returned {type(offered).__name__}, not a mapping\")", "note": "comparison flip: a well-formed mapping is refused, a malformed one proceeds"},
    {"id": "M10", "file": "APITool/cli.py", "original": "        out[str(key)] = command\n    return out", "mutated": "        out[key] = command\n    return out", "note": "dropped normalization: raw key type kept"},
    {"id": "M11", "file": "APITool/cli.py", "original": "    enabled = entry.target is not None", "mutated": "    enabled = entry.target is None", "note": "None flip: a configured plugin reads as disabled and vice versa"},
    {"id": "M12", "file": "APITool/cli.py", "original": "    if not enabled and not command.safe_unconfigured:\n        print(f\"Error: `plugins {name} {word}` needs the {name} plugin enabled, and no \"", "mutated": "    if not enabled or not command.safe_unconfigured:\n        print(f\"Error: `plugins {name} {word}` needs the {name} plugin enabled, and no \"", "note": "and/or swap: an unsafe command is refused even on an enabled plugin; a safe one is refused on an unenabled plugin"}
  ]
}
