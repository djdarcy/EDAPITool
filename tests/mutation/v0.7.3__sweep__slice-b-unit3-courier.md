# Mutation sweep — slice B, unit 3: the courier removed

**Project:** EDAPITool at `0a745ea` (v0.7.2) plus the uncommitted slice-B diff, shipping as v0.7.3
**Python:** 3.12.0 · **Platform:** Windows-11-10.0.26200 · **Date:** 2026-09-18
**Harness:** `~/.claude/skills/test-mutation/mutation_sweep.py` · **Generation mode:** 1 (fresh subagent, source + contract, zero tools)
**Scope:** the settlement plugin's `markers_for`, `layout(empty_marker=)`, `formula_help`; `cli.py`'s `--show-formula` block and the flag pass-through into `_plugin_layout`
**Tests per mutant:** `tests/test_plugin_loader.py`

Four behaviour reverts had already been run by the red-green audit (`redgreen_sliceb_unit3_courier.py`) and were excluded. One generated mutant (m8, the `--need-sign` mapping) lies on a line the unit did not change; it was kept as a free check on an adjacent pass-through and is reported as such.

## Round 1 — 8 mutants: 5 killed / 3 survived

| ID | File | Verdict | What it did |
|---|---|---|---|
| m1 | settlement/__init__.py | killed | the hollow guard inverted |
| m2 | settlement/__init__.py | killed | small and dotted swapped |
| m3 | settlement/__init__.py | killed | EMPTY written to the ENOUGH key |
| m4 | settlement/__init__.py | **survived** | `and`→`or`: a family name overwrites an explicit markers map |
| m5 | settlement/__init__.py | killed | only the None overrides kept |
| m6 | cli.py | **survived** | the `getattr` default for `show_formula` flipped |
| m7 | cli.py | killed | the callable check inverted |
| m8 | cli.py | **survived** | outside the diff: `--need-sign` mapping inverted |

## Triage

| Survivor | Verdict | Reasoning | Killing test |
|---|---|---|---|
| m4 | killable | nothing asserted that an explicit `markers=` wins over `empty_marker=` | `test_an_explicit_markers_map_wins_over_a_family_name` |
| m6 | **equivalent** | `--show-formula` is `action="store_true"` on the market parser (`cli.py:1323`), so the attribute always exists on a parsed namespace and the default is never consulted | — (`equivalents.md`) |
| m8 | killable — **a gap outside this unit** | no test drove `--need-sign` through `cmd_market`; the same capture-and-abort test also pins the `empty_marker` pass-through the audit could not reach | `test_the_cli_passes_its_flags_to_the_plugins_layout_as_words` |

## Round 2 — 2 mutants: 2 killed / 0 survived

## Restore

Both rounds: every restore `ok`, no `MUTANT` marker, porcelain identical apart from `tests/mutation/`. Full suite after: **690 passed**.

## Caveats

A kill proves the suite noticed *this* change, not that it tests the right thing — and **a clean sweep is evidence about the mutations tried, not proof of suite completeness.** m8 is worth noting: an excerpt slightly wider than the diff let the generator find a pre-existing gap in the CLI's flag mapping at no extra cost.

## Files

- `v0.7.3__spec__slice-b-unit3-courier.json` / `v0.7.3__report__slice-b-unit3-courier.json` — round 1
- `v0.7.3__spec__slice-b-unit3-courier-round2.json` / `v0.7.3__report__slice-b-unit3-courier-round2.json` — round 2
- `equivalents.md` — m6, under `cli.py`
