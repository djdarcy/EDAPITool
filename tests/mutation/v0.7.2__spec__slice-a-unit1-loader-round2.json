{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_plugin_loader.py", "tests/test_destination_seam.py"],
  "mutations": [
    {"id": "loader-loadresult-first-index", "file": "APITool/loader.py", "original": "    def first(self) -> Optional[Loaded]:\n        \"\"\"The plugin a single-destination command talks to, or None.\"\"\"\n        return self.loaded[0] if self.loaded else None", "mutated": "    def first(self) -> Optional[Loaded]:\n        \"\"\"The plugin a single-destination command talks to, or None.\"\"\"\n        return self.loaded[-1] if self.loaded else None", "note": "Round 1 survivor: first() returns the last-loaded plugin."},
    {"id": "loader-discover-drop-guard", "file": "APITool/loader.py", "original": "    if user_dir is None:\n        user_dir = settings.get_plugin_dir()\n    found = scan(SHIPPED_DIR, user_dir)", "mutated": "    user_dir = settings.get_plugin_dir()\n    found = scan(SHIPPED_DIR, user_dir)", "note": "Round 1 survivor: discover() ignores a caller-supplied user_dir."},
    {"id": "settings-plugin-dir-drop-expanduser-env", "file": "APITool/settings.py", "original": "    env = os.environ.get(\"ED_PLUGIN_DIR\")\n    if env:\n        return Path(env).expanduser()", "mutated": "    env = os.environ.get(\"ED_PLUGIN_DIR\")\n    if env:\n        return Path(env)", "note": "Round 1 survivor: drops expanduser on the env path."},
    {"id": "settings-plugin-dir-coerce-instead-of-refuse", "file": "APITool/settings.py", "original": "        if not isinstance(saved, str) or not saved:\n            raise ValueError(f'\"plugin_dir\" must be a non-empty string, got {saved!r}')\n        return Path(saved).expanduser()", "mutated": "        if not isinstance(saved, str) or not saved:\n            return PLUGIN_DIR\n        return Path(saved).expanduser()", "note": "Round 1 survivor's replacement: the corrected code silently falls back instead of refusing."},
    {"id": "settings-plugin-dir-drop-expanduser-file", "file": "APITool/settings.py", "original": "        return Path(saved).expanduser()\n    return PLUGIN_DIR", "mutated": "        return Path(saved)\n    return PLUGIN_DIR", "note": "New: drops expanduser on the saved path."},
    {"id": "settlement-layout-truthy", "file": "APITool/plugins/settlement/__init__.py", "original": "def layout(**overrides) -> SheetLayout:\n    \"\"\"This workbook's layout. Overrides are the command line's, when given.\"\"\"\n    return SheetLayout(**{k: v for k, v in overrides.items() if v is not None})", "mutated": "def layout(**overrides) -> SheetLayout:\n    \"\"\"This workbook's layout. Overrides are the command line's, when given.\"\"\"\n    return SheetLayout(**{k: v for k, v in overrides.items() if v})", "note": "Round 1 survivor: is-not-None becomes truthiness."}
  ]
}
