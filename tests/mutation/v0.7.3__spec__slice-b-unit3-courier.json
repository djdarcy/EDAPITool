{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_plugin_loader.py"],
  "mutations": [
    {"id": "m1", "file": "APITool/plugins/settlement/__init__.py", "original": "    if empty_marker in (None, \"hollow\"):\n        return None", "mutated": "    if empty_marker not in (None, \"hollow\"):\n        return None", "note": "The hollow guard inverted."},
    {"id": "m2", "file": "APITool/plugins/settlement/__init__.py", "original": "    empty = MARKER_EMPTY_SMALL if empty_marker == \"small\" else MARKER_EMPTY_DOTTED\n    return {", "mutated": "    empty = MARKER_EMPTY_DOTTED if empty_marker == \"small\" else MARKER_EMPTY_SMALL\n    return {", "note": "small and dotted swapped."},
    {"id": "m3", "file": "APITool/plugins/settlement/__init__.py", "original": "        MatchState.PARTIAL: MARKER_PARTIAL,\n        MatchState.EMPTY: empty,\n    }", "mutated": "        MatchState.PARTIAL: MARKER_PARTIAL,\n        MatchState.ENOUGH: empty,\n    }", "note": "Wrong dict key: EMPTY written to ENOUGH."},
    {"id": "m4", "file": "APITool/plugins/settlement/__init__.py", "original": "    if empty_marker is not None and \"markers\" not in values:\n        values[\"markers\"] = markers_for(empty_marker)", "mutated": "    if empty_marker is not None or \"markers\" not in values:\n        values[\"markers\"] = markers_for(empty_marker)", "note": "and->or: markers_for(None) overwrites an explicit markers override."},
    {"id": "m5", "file": "APITool/plugins/settlement/__init__.py", "original": "    values = {k: v for k, v in overrides.items() if v is not None}", "mutated": "    values = {k: v for k, v in overrides.items() if v is None}", "note": "Keeps only the None overrides."},
    {"id": "m6", "file": "APITool/cli.py", "original": "    if getattr(args, \"show_formula\", False):\n        # The formula reproduces the plugin's own glyphs and thresholds, so", "mutated": "    if getattr(args, \"show_formula\", True):\n        # The formula reproduces the plugin's own glyphs and thresholds, so", "note": "The getattr default flipped."},
    {"id": "m7", "file": "APITool/cli.py", "original": "        if not callable(formula_help):\n            print(f\"The {destination.name!r} plugin has no formula help.\")\n            return 1", "mutated": "        if callable(formula_help):\n            print(f\"The {destination.name!r} plugin has no formula help.\")\n            return 1", "note": "The callable check inverted."},
    {"id": "m8", "file": "APITool/cli.py", "original": "        need_sign=SIGN_NEGATIVE if args.need_sign == \"negative\" else SIGN_POSITIVE,\n        marker_column=args.marker_column,", "mutated": "        need_sign=SIGN_POSITIVE if args.need_sign == \"negative\" else SIGN_NEGATIVE,\n        marker_column=args.marker_column,", "note": "Outside the unit's diff: the --need-sign mapping inverted."}
  ]
}
