{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider",
                   "tests/test_config_regions.py", "tests/test_plugin_loader.py",
                   "tests/test_settings.py", "tests/test_supplier_registry.py"],
  "mutations": [
    {"id": "S1", "file": "APITool/settings.py",
     "original": "        if target.kind == \"gsheet\" and target.params.get(\"id\"):\n            return target.params[\"id\"]",
     "mutated": "        if target.kind != \"gsheet\" and target.params.get(\"id\"):\n            return target.params[\"id\"]",
     "note": "Comparison flip: loop now returns a non-gsheet target's id instead of a gsheet target's id."},
    {"id": "S2", "file": "APITool/plugins/settlement/bindings.py",
     "original": "        out.append((Destination.parse(entry[\"region\"]),\n                    (entry.get(\"site\") or None)))",
     "mutated": "        out.append((Destination.parse(entry[\"region\"]),\n                    entry.get(\"site\")))",
     "note": "Drops the or-None normalization, so an explicit empty-string site is kept instead of becoming None."},
    {"id": "S3", "file": "APITool/settings.py",
     "original": "    if not sheet_id and not extra:\n        return None",
     "mutated": "    if not sheet_id or not extra:\n        return None",
     "note": "And/or swap in default_target's early-return guard."},
    {"id": "S4", "file": "APITool/settings.py",
     "original": "CORE_KEYS = frozenset({\"client_id\", \"sheet_id\", \"plugin_dir\", \"targets\"})",
     "mutated": "CORE_KEYS = frozenset({\"client_id\", \"sheet_id\", \"targets\"})",
     "note": "Drops plugin_dir from CORE_KEYS, so it leaks into the default target's config block."},
    {"id": "S5", "file": "APITool/settings.py",
     "original": "        params = {k: v for k, v in entry.items() if k not in (\"kind\", \"plugin\", \"config\")}",
     "mutated": "        params = {k: v for k, v in entry.items() if k not in (\"kind\", \"plugin\")}",
     "note": "Dropped dict-key filter: config is duplicated into params."},
    {"id": "S6", "file": "APITool/loader.py",
     "original": "        kinds.setdefault(target.plugin, target.kind)",
     "mutated": "        kinds[target.plugin] = target.kind",
     "note": "First-vs-last: the LAST target naming a plugin wins its kind."},
    {"id": "S7", "file": "APITool/loader.py",
     "original": "        by_plugin.setdefault(target.plugin, target)",
     "mutated": "        by_plugin[target.plugin] = target",
     "note": "First-vs-last: the LAST target naming a plugin wins."},
    {"id": "S8", "file": "APITool/loader.py",
     "original": "    if targets:\n        return _unique(t.plugin for t in targets.values())",
     "mutated": "    if not targets:\n        return _unique(t.plugin for t in targets.values())",
     "note": "Inverted guard in enabled_from: configured targets ignored, shipped fallback used, and vice versa."},
    {"id": "S9", "file": "APITool/loader.py",
     "original": "    targets = settings.get_targets()\n    if not targets:",
     "mutated": "    targets = settings.get_targets()\n    if targets:",
     "note": "Inverted guard in discover: the legacy alias overwrites configured targets."},
    {"id": "S10", "file": "APITool/cli.py",
     "original": "    block = destination.target.config if destination.target is not None else {}",
     "mutated": "    block = destination.target.config if destination.target is None else {}",
     "note": "Inverted None-check: the real block is dropped when a target exists."},
    {"id": "S11", "file": "APITool/cli.py",
     "original": "        if not args.construction_region:\n            print(f\"       (from {settings.CONFIG_FILE})\")",
     "mutated": "        if args.construction_region:\n            print(f\"       (from {settings.CONFIG_FILE})\")",
     "note": "Inverted guard: the config-file hint prints only for the flag case."},
    {"id": "S12", "file": "APITool/sheets/reader.py",
     "original": "        prefix = f\"{self.target}!\" if self.target else \"\"",
     "mutated": "        prefix = f\"{self.target}!\" if not self.target else \"\"",
     "note": "Inverted ternary: prefix only when there is no target."}
  ]
}
