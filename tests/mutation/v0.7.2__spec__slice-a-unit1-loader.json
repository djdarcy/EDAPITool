{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_plugin_loader.py", "tests/test_destination_seam.py"],
  "mutations": [
    {"id": "loader-scan-loop-order", "file": "APITool/loader.py", "original": "    by_name: dict[str, Found] = {}\n    for child in _candidates(shipped_dir):\n        by_name[child.name] = Found(child.name, child, ORIGIN_SHIPPED)\n    for child in _candidates(user_dir):\n        hidden = by_name.get(child.name)\n        by_name[child.name] = Found(\n            child.name, child, ORIGIN_USER,\n            shadows=hidden.location if hidden is not None else None,\n        )", "mutated": "    by_name: dict[str, Found] = {}\n    for child in _candidates(user_dir):\n        hidden = by_name.get(child.name)\n        by_name[child.name] = Found(\n            child.name, child, ORIGIN_USER,\n            shadows=hidden.location if hidden is not None else None,\n        )\n    for child in _candidates(shipped_dir):\n        by_name[child.name] = Found(child.name, child, ORIGIN_SHIPPED)", "note": "Reverses which source is scanned first: shipped clobbers user, shadowing never fires."},
    {"id": "loader-import-user-spec-and", "file": "APITool/loader.py", "original": "    if spec is None or spec.loader is None:\n        raise ImportError(f\"no importable package at {found.location}\")", "mutated": "    if spec is None and spec.loader is None:\n        raise ImportError(f\"no importable package at {found.location}\")", "note": "or->and: a spec with a None loader skips the guard."},
    {"id": "loader-import-user-drop-cleanup", "file": "APITool/loader.py", "original": "    try:\n        spec.loader.exec_module(module)\n    except BaseException:\n        # A half-imported module left in sys.modules would make the NEXT\n        # attempt look like a success. Take it back out.\n        sys.modules.pop(qualified, None)\n        raise\n    return module", "mutated": "    try:\n        spec.loader.exec_module(module)\n    except BaseException:\n        # A half-imported module left in sys.modules would make the NEXT\n        # attempt look like a success. Take it back out.\n        raise\n    return module", "note": "Drops the sys.modules cleanup on a failed import."},
    {"id": "loader-unique-flip", "file": "APITool/loader.py", "original": "def _unique(names: Iterable[str]) -> list[str]:\n    seen: list[str] = []\n    for name in names:\n        if name not in seen:\n            seen.append(name)\n    return seen", "mutated": "def _unique(names: Iterable[str]) -> list[str]:\n    seen: list[str] = []\n    for name in names:\n        if name in seen:\n            seen.append(name)\n    return seen", "note": "Inverts the dedup membership check."},
    {"id": "loader-load-swap-importer", "file": "APITool/loader.py", "original": "        importer = _import_user if entry.origin == ORIGIN_USER else _import_shipped\n        try:\n            module = importer(entry)", "mutated": "        importer = _import_shipped if entry.origin == ORIGIN_USER else _import_user\n        try:\n            module = importer(entry)", "note": "Swaps the two importers."},
    {"id": "loader-load-available-negation", "file": "APITool/loader.py", "original": "    result.available = [f for f in found if f.name not in wanted]\n    return result", "mutated": "    result.available = [f for f in found if f.name in wanted]\n    return result", "note": "available lists the enabled instead of the not-enabled."},
    {"id": "loader-loadresult-first-index", "file": "APITool/loader.py", "original": "    def first(self) -> Optional[Loaded]:\n        \"\"\"The plugin a single-destination command talks to, or None.\"\"\"\n        return self.loaded[0] if self.loaded else None", "mutated": "    def first(self) -> Optional[Loaded]:\n        \"\"\"The plugin a single-destination command talks to, or None.\"\"\"\n        return self.loaded[-1] if self.loaded else None", "note": "first() returns the last-loaded plugin."},
    {"id": "loader-discover-drop-guard", "file": "APITool/loader.py", "original": "    if user_dir is None:\n        user_dir = settings.get_plugin_dir()\n    found = scan(SHIPPED_DIR, user_dir)", "mutated": "    user_dir = settings.get_plugin_dir()\n    found = scan(SHIPPED_DIR, user_dir)", "note": "discover() ignores a caller-supplied user_dir."},
    {"id": "settings-plugin-dir-drop-expanduser-env", "file": "APITool/settings.py", "original": "    env = os.environ.get(\"ED_PLUGIN_DIR\")\n    if env:\n        return Path(env).expanduser()", "mutated": "    env = os.environ.get(\"ED_PLUGIN_DIR\")\n    if env:\n        return Path(env)", "note": "Drops expanduser on the env path."},
    {"id": "settings-plugin-dir-drop-str-saved", "file": "APITool/settings.py", "original": "    saved = load().get(\"plugin_dir\")\n    if saved:\n        return Path(str(saved)).expanduser()", "mutated": "    saved = load().get(\"plugin_dir\")\n    if saved:\n        return Path(saved).expanduser()", "note": "Drops str() on the saved plugin_dir."},
    {"id": "cli-resolve-destination-invert-guard", "file": "APITool/cli.py", "original": "    destination = plugins.first()\n    if destination is None:\n        lines = [\"Error: no destination plugin is loaded.\"]\n        lines += [f\"  {line}\" for line in plugins.describe()]\n        return None, \"\\n\".join(lines)\n    return destination, None", "mutated": "    destination = plugins.first()\n    if destination is not None:\n        lines = [\"Error: no destination plugin is loaded.\"]\n        lines += [f\"  {line}\" for line in plugins.describe()]\n        return None, \"\\n\".join(lines)\n    return destination, None", "note": "Inverts the no-destination guard."},
    {"id": "settlement-layout-truthy", "file": "APITool/plugins/settlement/__init__.py", "original": "def layout(**overrides) -> SheetLayout:\n    \"\"\"This workbook's layout. Overrides are the command line's, when given.\"\"\"\n    return SheetLayout(**{k: v for k, v in overrides.items() if v is not None})", "mutated": "def layout(**overrides) -> SheetLayout:\n    \"\"\"This workbook's layout. Overrides are the command line's, when given.\"\"\"\n    return SheetLayout(**{k: v for k, v in overrides.items() if v})", "note": "is-not-None becomes truthiness: a falsy override is dropped."}
  ]
}
