{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_writer_ledger.py", "tests/test_marker_skip_occupied.py", "tests/test_location_opt_in.py", "tests/test_sheets.py"],
  "mutations": [
    {"id": "M01", "file": "APITool/sheets/writer.py", "original": "            if force:\n                current, readable = {}, True          # --force reads nothing", "mutated": "            if not force:\n                current, readable = {}, True          # --force reads nothing", "note": "--force gate flipped"},
    {"id": "M02", "file": "APITool/sheets/writer.py", "original": "                if self.ledger is not None and not force else {}", "mutated": "                if self.ledger is not None or not force else {}", "note": "and/or swap on the ledger lookup"},
    {"id": "M03", "file": "APITool/sheets/writer.py", "original": "            verdict = classify(current, recorded, planned, force=force, readable=readable)", "mutated": "            verdict = classify(recorded, current, planned, force=force, readable=readable)", "note": "current and recorded swapped"},
    {"id": "M04", "file": "APITool/sheets/writer.py", "original": "        for cell in location:\n            if cell not in held:", "mutated": "        for cell in location:\n            if cell in held:", "note": "held filter on location inverted"},
    {"id": "M05", "file": "APITool/sheets/writer.py", "original": "        if include_markers:\n            if last >= first:", "mutated": "        if include_markers:\n            if last > first:", "note": "single-row marker case dropped"},
    {"id": "M06", "file": "APITool/sheets/writer.py", "original": "                        f for f in plan.formats if f[\"range\"] not in held", "mutated": "                        f for f in plan.formats if f[\"range\"] in held", "note": "formats filter inverted"},
    {"id": "M07", "file": "APITool/sheets/writer.py", "original": "            current[cell] = rows[0][0] if rows and rows[0] else \"\"", "mutated": "            current[cell] = rows[0][0] if rows or rows[0] else \"\"", "note": "and/or swap on a single cell's answer"},
    {"id": "M08", "file": "APITool/sheets/writer.py", "original": "            rows = answers[len(cells)] if len(answers) > len(cells) else []", "mutated": "            rows = answers[len(cells)] if len(answers) >= len(cells) else []", "note": "off-by-one on the marker answer"},
    {"id": "M09", "file": "APITool/sheets/writer.py", "original": "                current[f\"{column}{first_row + offset}\"] = row[0] if row else \"\"\n        return current, True", "mutated": "                current[f\"{column}{first_row + offset}\"] = row[0] if row else \"\"\n        return current, False", "note": "a good read reported unreadable"},
    {"id": "M10", "file": "APITool/sheets/writer.py", "original": "        except Exception:  # noqa: BLE001 -- an unread cell is simply not recorded\n            return None", "mutated": "        except Exception:  # noqa: BLE001 -- an unread cell is simply not recorded\n            return {}", "note": "failed read-back returns {} instead of None"},
    {"id": "M11", "file": "APITool/sheets/writer.py", "original": "        if not governed or not ranges:\n            return", "mutated": "        if not governed and not ranges:\n            return", "note": "and/or swap on the record guard"},
    {"id": "M12", "file": "APITool/sheets/writer.py", "original": "        if self.ledger is None:\n            return", "mutated": "        if self.ledger is not None:\n            return", "note": "no-ledger guard inverted"}
  ]
}
