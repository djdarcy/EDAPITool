# v0.8.1 sweep -- unit 2 of slice 4 (whose cell is it: the pure rule)

## Environment

| | |
|---|---|
| Project | EDAPITool, uncommitted over `8c5772d` (v0.8.0) |
| Python | 3.12.0, Windows 11 (10.0.26200) |
| Harness | `mutation_sweep.py` 0.1.0 (test-mutation skill) |
| Generation | mode 1 -- fresh subagent (Sonnet), the source inline, no tools, no tests. The first generator returned prose twice; a second, with the output rule stated first, returned the JSON |
| Date | 2026-09-25 |
| Spec / report | `v0.8.1__spec__unit-2-classify.json`; `v0.8.1__report__unit-2-classify.json` |

Scope: `APITool/sheets/ledger.py` (new, pure). Focused tests: `tests/test_sheets_ledger.py`. No I/O is reachable from this module.

## Kill table

| ID | Mutation | Verdict |
|---|---|---|
| M01 | `is_empty`: `or` -> `and` | killed |
| M02 | `as_text`: branches swapped | killed |
| M03 | force guard inverted | killed |
| M04 | forced cells fall through to classification | killed |
| M05 | readable guard inverted | killed |
| M06 | empty check inverted | killed |
| M07 | refresh condition `and` -> `or` | killed |
| M08 | raw compare instead of text | killed |
| M09 | fallback fills instead of holding | killed |
| M10 | `written()` drops forced cells | **survived** |
| M11 | `written()` sorted instead of plan order | killed |
| M12 | `is_empty` drops `strip()` | killed |

11/12.

## Triage

| ID | Verdict | Reasoning |
|---|---|---|
| M10 | killable, resolved by deletion | Tracing the survivor showed that nothing in production called `Classification.written()`: the writer builds its updates from the held set. A test for it would have pinned an API with no caller. `written()` and the `_order` bookkeeping behind it were removed, M10 and M11 have nothing left to mutate, and the plan-order assertion now checks each list's own order. |

## What the sweep taught

- **A survivor can be telling you the code should not exist.** The red-green audit had already caught `written()`'s plan-order test attached to nothing (the fill was planned last, where concatenation agreed). This sweep found the deeper fact: the helper was designed for a caller that was never written.

## Caveats

A kill proves the suite noticed *this* change, not that it tests the right thing -- and **a clean sweep is evidence about the mutations tried, not proof of suite completeness.**
