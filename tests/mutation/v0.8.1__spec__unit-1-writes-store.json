{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_store_writes.py", "tests/test_store.py"],
  "mutations": [
    {"id": "M01", "file": "APITool/store/writes.py", "original": "    if not cells or os.environ.get(GUARD_VAR):\n        return {}", "mutated": "    if not cells and os.environ.get(GUARD_VAR):\n        return {}", "note": "and/or swap in recorded()'s guard"},
    {"id": "M02", "file": "APITool/store/writes.py", "original": "            out.update({a1: (\"\" if value is None else value) for a1, value in rows})", "mutated": "            out.update({a1: (\"\" if value is not None else value) for a1, value in rows})", "note": "None-normalisation inverted on read"},
    {"id": "M03", "file": "APITool/store/writes.py", "original": "            chunk = cells[start:start + 500]", "mutated": "            chunk = cells[start:start + 499]", "note": "each chunk drops its 500th cell"},
    {"id": "M04", "file": "APITool/store/writes.py", "original": "                (target, tab, *chunk))", "mutated": "                (tab, target, *chunk))", "note": "target and tab bindings swapped on read"},
    {"id": "M05", "file": "APITool/store/writes.py", "original": "    if not values or os.environ.get(GUARD_VAR):\n        return False", "mutated": "    if not values and os.environ.get(GUARD_VAR):\n        return False", "note": "and/or swap in record()'s guard"},
    {"id": "M06", "file": "APITool/store/writes.py", "original": "                [(target, tab, str(a1), \"\" if v is None else str(v), stamp, run_id)", "mutated": "                [(target, tab, str(a1), \"\" if v is not None else str(v), stamp, run_id)", "note": "None-normalisation inverted on write"},
    {"id": "M07", "file": "APITool/store/writes.py", "original": "        return True", "mutated": "        return False", "note": "record() reports failure after success"},
    {"id": "M08", "file": "APITool/store/writes.py", "original": "    def recorded(self, tab: str, a1s: Iterable[str]) -> dict[str, str]:\n        return recorded(self.target, tab, a1s)", "mutated": "    def recorded(self, tab: str, a1s: Iterable[str]) -> dict[str, str]:\n        return recorded(tab, self.target, a1s)", "note": "StoreLedger.recorded swaps target and tab"},
    {"id": "M09", "file": "APITool/store/writes.py", "original": "    def record(self, tab: str, values: Mapping[str, str]) -> bool:\n        return record(self.target, tab, values, self.run_id)", "mutated": "    def record(self, tab: str, values: Mapping[str, str]) -> bool:\n        return record(tab, self.target, values, self.run_id)", "note": "StoreLedger.record swaps target and tab"},
    {"id": "M10", "file": "APITool/store/writes.py", "original": "    own = conn is None\n    try:\n        if own:\n            conn = open_store()\n        out: dict[str, str] = {}", "mutated": "    own = conn is not None\n    try:\n        if own:\n            conn = open_store()\n        out: dict[str, str] = {}", "note": "ownership detection flipped in recorded()"},
    {"id": "M11", "file": "APITool/store/writes.py", "original": "        _report(f\"store: could not read the writes ledger: {exc}\")\n        return {}\n    finally:\n        if own and conn is not None:\n            conn.close()", "mutated": "        _report(f\"store: could not read the writes ledger: {exc}\")\n        return {}\n    finally:\n        if own or conn is not None:\n            conn.close()", "note": "recorded() closes a caller's connection"}
  ]
}
