{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_store_archive.py", "tests/test_store.py", "tests/test_journal.py"],
  "mutations": [
    {"id": "M01", "file": "APITool/journal.py", "original": "        if not path.is_file():", "mutated": "        if path.is_file():", "note": "Inverted file-existence guard: reads proceed only when the file is absent, so real Market/Cargo/Status files are skipped and missing ones are read."},
    {"id": "M02", "file": "APITool/journal.py", "original": "        if not isinstance(data, dict):", "mutated": "        if isinstance(data, dict):", "note": "Inverted type check: valid dict payloads are now rejected (return None) while non-dict payloads fall through to _archive/return."},
    {"id": "M03", "file": "APITool/journal.py", "original": "        subject = data.get(\"MarketID\") if kind == \"market_json\" else data.get(\"Vessel\")", "mutated": "        subject = data.get(\"Vessel\") if kind == \"market_json\" else data.get(\"MarketID\")", "note": "Swapped dict keys between branches: market_json archives get the (missing) Vessel key as subject and vice versa, so subject is wrong/None."},
    {"id": "M04", "file": "APITool/journal.py", "original": "        if name == \"LoadGame\":", "mutated": "        if name != \"LoadGame\":", "note": "Comparison flip: commander/FID are updated on every event except LoadGame, instead of only on LoadGame."},
    {"id": "M05", "file": "APITool/journal.py", "original": "            self.commander = event.get(\"Commander\") or self.commander", "mutated": "            self.commander = event.get(\"Commander\") and self.commander", "note": "or->and: effectively clears commander on first LoadGame."},
    {"id": "M06", "file": "APITool/capi.py", "original": "            return str(data[\"id\"])", "mutated": "            return str(data[\"name\"])", "note": "Wrong dict key: capi_market subject lookup reads 'name' instead of 'id'."},
    {"id": "M07", "file": "APITool/capi.py", "original": "        if endpoint == path or endpoint.startswith(path + \"/\"):", "mutated": "        if endpoint == path and endpoint.startswith(path + \"/\"):", "note": "or->and: an endpoint that exactly equals a known path no longer matches, so exact-path CAPI responses are never archived."},
    {"id": "M08", "file": "APITool/capi.py", "original": "            return str(data[\"commander\"][\"name\"])", "mutated": "            return str(data[\"commander\"][\"id\"])", "note": "Wrong dict key: capi_profile subject now reads commander id instead of commander name."},
    {"id": "M09", "file": "APITool/capi.py", "original": "                _archive(endpoint, response.content, data, url)", "mutated": "                _archive(endpoint, data, response.content, url)", "note": "Swapped arguments: raw bytes and parsed data are passed in the wrong positions."},
    {"id": "M10", "file": "APITool/store/__init__.py", "original": "    if os.environ.get(GUARD_VAR):", "mutated": "    if not os.environ.get(GUARD_VAR):", "note": "Inverted guard: archiving is skipped whenever the guard var is unset and proceeds only when it is set."},
    {"id": "M11", "file": "APITool/store/__init__.py", "original": "                (source_id, kind, subject, observed_at or stamp, stamp, digest, payload))", "mutated": "                (source_id, kind, subject, observed_at and stamp, stamp, digest, payload))", "note": "or->and: the stored observed_at becomes the recording stamp when the payload carried its own timestamp, and None when it did not."},
    {"id": "M12", "file": "APITool/store/__init__.py", "original": "            project_one(conn, obs_id, kind, payload, observed_at or stamp)", "mutated": "            project_one(conn, obs_id, kind, payload, observed_at and stamp)", "note": "or->and applied to the projection timestamp."}
  ]
}
