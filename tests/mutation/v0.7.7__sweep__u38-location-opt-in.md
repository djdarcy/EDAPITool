# v0.7.7 sweep -- the location cells are opt-in (U38, #25)

## Environment

| | |
|---|---|
| Project | EDAPITool, uncommitted diff over `1f44da5` (v0.7.6) |
| Python | 3.12.0, Windows 11 (10.0.26200) |
| Harness | `mutation_sweep.py` (test-mutation skill) |
| Generation | mode 1 -- fresh subagent, source inline, no tools, no tests |
| Date | 2026-09-24 |
| Spec / report | `v0.7.7__spec__u38-location-opt-in.json`, `v0.7.7__report__u38-location-opt-in.json` |

Scope: the lines U38 added to `sheets/writer.py`, `plugins/settlement/__init__.py`, `service.py` and `daemon.py`. `cli.py` was not mutated: its two new lines are textually identical to `serve`'s existing ones, so no unique anchor existed; the CLI arm of the red-green audit (`tests/one-offs/thinking/redgreen_u38_write_location.py`, arm d) covers `market` passing the flag.

## Kill table

| ID | File | Mutation | First sweep | After M9's test |
|---|---|---|---|---|
| M1 | writer.py | `write_location` default -> True | killed | killed |
| M2 | writer.py | `if write_location` -> `if not` | killed | killed |
| M3 | writer.py | guard -> `if True` | killed | killed |
| M4 | writer.py | system-cell append dropped | killed | killed |
| M5 | writer.py | station-cell append dropped | killed | killed |
| M6 | writer.py | system value sent to the station cell | killed | killed |
| M7 | settlement | `.get("write_location", True)` | killed | killed |
| M8 | settlement | option not passed | killed | killed |
| M9 | service.py | `refresh()` default -> True | **survived** | killed |
| M10 | service.py | option not carried into the context | killed | killed |
| M11 | daemon.py | `is not None` -> `is None` | killed | killed |
| M12 | daemon.py | serve stops passing the option | killed | killed |

## Triage

| ID | Verdict | Reasoning | Killing test |
|---|---|---|---|
| M9 | killable | Both shipped callers pass the option explicitly, so `refresh()`'s own default was exercised by nothing -- a future caller relying on it would silently overwrite C2/G2 | `tests/test_service.py::test_a_refresh_that_does_not_ask_for_location_writes_none` -- red under M9, green on the real code |

No refinement round was generated: the one killable survivor was closed, and the first round already covered every line the unit added.

## Caveats

A kill proves the suite noticed *this* change, not that it tests the right thing. **A clean sweep is evidence about the mutations tried, not proof of suite completeness.**
