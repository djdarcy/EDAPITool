# Mutation sweep — slice B, unit 2: the supplier/subscriber registry

**Project:** EDAPITool at `0a745ea` (v0.7.2) plus the uncommitted slice-B diff, shipping as v0.7.3
**Python:** 3.12.0 · **Platform:** Windows-11-10.0.26200 · **Date:** 2026-09-18
**Harness:** `~/.claude/skills/test-mutation/mutation_sweep.py` · **Generation mode:** 1 (fresh subagent, source + contract, zero tools)
**Scope:** `APITool/registry.py` (new), `service.py`'s registry block, `refresh` and `_finish`, the settlement plugin's `supplies()`/`subscribes()`, `matcher.py`'s `origin`, the reader's `origin_for`
**Tests per mutant:** `tests/test_supplier_registry.py tests/test_service.py tests/test_sheets.py`

Nine behaviour reverts had already been run by the red-green audit (`redgreen_sliceb_unit2_registry.py`) and were excluded.

**Blast radius:** counters and in-memory fakes throughout; the service-level tests write to a fake sheet from a journal in `tmp_path`. The self-referential market supplier is a `RecursionError`, not a hang.

## Round 1 — 10 mutants: 8 killed / 2 survived

| ID | File | Verdict | What it did |
|---|---|---|---|
| reg-get-wrongkey | registry.py | killed | `get()` looked in the cache instead of the suppliers |
| reg-run-reversed | registry.py | killed | subscriptions run in reverse |
| reg-merge-condition-flip | registry.py | killed | the duplicate-offer guard inverted |
| reg-supplied-wrongkey | registry.py | killed | `supplied()` listed every supplier |
| svc-market-supplier-wrongkey | service.py | killed | the market supplier asked for itself |
| svc-refresh-marketnone-flip | service.py | killed | bailed out when the market was found |
| svc-refresh-or-and-swap | service.py | killed | the no-comparison early return narrowed |
| svc-compare-swapped-args | service.py | killed | `compare()` arguments swapped |
| svc-finish-and-or-swap | service.py | **survived** | `_finish` pulled requirements nobody supplied |
| svc-finish-elif-flip | service.py | **survived** | `checked_at` stamped only on failure |

## Triage

| Survivor | Verdict | Reasoning | Killing test |
|---|---|---|---|
| finish-and-or-swap | killable | the quiet-plugin test used the docked path, which returns before `_finish`; the not-docked path reaches it | `test_a_plugin_that_supplies_nothing_survives_the_not_docked_path` |
| finish-elif-flip | killable | no test had a successful refresh whose market carried no timestamp; the fallback stamp was never observed | `test_checked_at_is_now_when_the_refresh_succeeded_without_a_market_timestamp` — a probe subscription records `ctx.checked_at` |

## Round 2 — 2 mutants: 2 killed / 0 survived

## Restore

Both rounds: every restore `ok`, no `MUTANT` marker, porcelain identical apart from `tests/mutation/`. Full suite after: **682 passed**.

## Caveats

A kill proves the suite noticed *this* change, not that it tests the right thing — and **a clean sweep is evidence about the mutations tried, not proof of suite completeness.** The audit's nine and the sweep's ten overlapped nowhere; the two gaps the sweep found are both paths the audit's reverts did not take.

## Files

- `v0.7.3__spec__slice-b-unit2-registry.json` / `v0.7.3__report__slice-b-unit2-registry.json` — round 1
- `v0.7.3__spec__slice-b-unit2-registry-round2.json` / `v0.7.3__report__slice-b-unit2-registry-round2.json` — round 2
- `equivalents.md` — no new entries
