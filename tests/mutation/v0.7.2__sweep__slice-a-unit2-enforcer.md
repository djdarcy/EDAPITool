# Mutation sweep — slice A, unit 2: core builds the enforcer from the declaration

**Project:** EDAPITool at `aaf0126` (v0.7.1) plus the uncommitted slice-A diff, shipping as v0.7.2
**Python:** 3.12.0 · **Platform:** Windows-11-10.0.26200 · **Date:** 2026-09-18
**Harness:** `~/.claude/skills/test-mutation/mutation_sweep.py` (per-mutant `PYTHONPYCACHEPREFIX`, byte-verified restore, survivor re-verify)
**Generation mode:** 1 — fresh subagent, source + contract only, zero tools
**Scope:** the kinds section of `APITool/loader.py`, the guard parameter and fallback in `APITool/service.py`, `TotalsTabWriter.__init__`, `SheetLayout.writes()`, the two `build_enforcer` sites in `cli.py`, the guard pass-through in `daemon.py`
**Tests per mutant:** `tests/test_plugin_loader.py tests/test_service.py tests/test_sheets.py`

Six behaviour reverts had already been run by the red-green audit (`tests/one-offs/thinking/plugin-isolation/redgreen_unit2_enforcer.py`) and were excluded from generation.

**Blast radius, read before the sweep:** the guard prevents writes outside a declaration. Every test in scope writes to an in-memory fake; no gspread client is constructed in these files. `daemon.build()` constructs a real exporter, and no test in scope calls it — so its mutant could only survive, never start anything.

## Round 1 — 10 mutants: 7 killed / 3 survived

| ID | File | Verdict | What it did |
|---|---|---|---|
| loader-kindfor-andor | loader.py | killed | unknown kind dies with `KeyError`, not the promised `ValueError` |
| loader-build-enforcer-swapped-args | loader.py | killed | kind and declaration swapped |
| loader-gsheetkind-drop-dict-copy | loader.py | **survived** | `dict()` normalisation dropped |
| loader-load-kind-precedence-andor | loader.py | killed | configured kind loses to the plugin's own |
| loader-discover-swapped-args | loader.py | killed | enabled list and kinds map swapped |
| service-guard-truthiness | service.py | **survived** | `is None` → `not guard` |
| layout-writes-wrong-dict-key | layout.py | killed | the hand-maintained tab declared whole-tab writable |
| totals-layout-andor | totals.py | killed | a passed layout discarded for the default |
| cli-cmd-market-swapped-args | cli.py | killed | kind and declaration swapped at the composition root |
| daemon-drop-guard-passthrough | daemon.py | **survived** | the composition root's guard dropped on the way into the service |

## Triage

| Survivor | Verdict | Reasoning | Killing test |
|---|---|---|---|
| gsheetkind-drop-dict-copy | **equivalent** | `WriteGuard.build` only calls `.items()`; any `Mapping` behaves identically to its `dict` copy | — (`equivalents.md`) |
| service-guard-truthiness | **equivalent** | `WriteGuard` is a plain frozen dataclass, always truthy (`bool(WriteGuard.build({}))` is `True`); both tests select the same branch for anything a caller can pass | — (`equivalents.md`) |
| daemon-drop-guard-passthrough | killable | no test calls `daemon.build`, because it constructs a real exporter. Capture-and-abort: a recording stand-in for the service raises before the exporter is reached | `test_the_daemon_hands_the_composition_roots_guard_to_the_service` |

## Round 2 — 1 mutant: 1 killed / 0 survived

The daemon pass-through re-applied against the new test: killed. Two rounds is the cap; the sweep is closed.

## Restore

Both rounds: every restore `ok`, no `MUTANT` marker under `APITool/`, `git status --porcelain` identical to the pre-sweep capture apart from `tests/mutation/`. Full suite after: **634 passed**.

## Caveats

A kill proves the suite noticed *this* change, not that it tests the right thing — and **a clean sweep is evidence about the mutations tried, not proof of suite completeness.** The audit and the sweep disagreed nowhere; the sweep found one gap (the daemon pass-through) that the audit's hand-picked six did not reach, because the audit reverted behaviour inside functions tests already called and the sweep attacked a function none did.

## Files

- `v0.7.2__spec__slice-a-unit2-enforcer.json` / `v0.7.2__report__slice-a-unit2-enforcer.json` — round 1
- `v0.7.2__spec__slice-a-unit2-enforcer-round2.json` / `v0.7.2__report__slice-a-unit2-enforcer-round2.json` — round 2
- `equivalents.md` — two equivalents, under `loader.py @ 93611c219003` and `service.py @ 02e54c3e8b91`
