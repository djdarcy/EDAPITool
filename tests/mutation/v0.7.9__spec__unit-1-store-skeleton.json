{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_store.py", "tests/test_stock_config.py"],
  "mutations": [
    {"id": "M01", "file": "APITool/store/__init__.py", "original": "    if version == SCHEMA_VERSION:", "mutated": "    if version >= SCHEMA_VERSION:", "note": "Comparison flip == -> >=; a store with a newer schema than this tool now silently returns instead of raising StoreVersionError."},
    {"id": "M02", "file": "APITool/store/__init__.py", "original": "    if version > SCHEMA_VERSION:", "mutated": "    if version < SCHEMA_VERSION:", "note": "Comparison flip > -> <; the newer-schema guard now fires for an older schema and lets a genuinely newer schema fall through into the migration loop."},
    {"id": "M03", "file": "APITool/store/__init__.py", "original": "                for step in range(version + 1, SCHEMA_VERSION + 1):", "mutated": "                for step in range(version, SCHEMA_VERSION + 1):", "note": "Off-by-one: drops the +1, so the migration for the already-applied current version is re-run."},
    {"id": "M04", "file": "APITool/store/__init__.py", "original": "        for table in reversed(derived):", "mutated": "        for table in derived:", "note": "Reversed iteration dropped when dropping derived tables before rebuild; can violate FK ordering during teardown."},
    {"id": "M05", "file": "APITool/store/__init__.py", "original": "                \"INSERT OR IGNORE INTO sources (kind, machine, commander, commander_fid, \"", "mutated": "                \"INSERT OR REPLACE INTO sources (kind, machine, commander, commander_fid, \"", "note": "INSERT OR IGNORE -> INSERT OR REPLACE; a re-seen source row is now clobbered (losing first_seen) instead of left alone."},
    {"id": "M06", "file": "APITool/store/market.py", "original": "    return (match.group(1) if match else (symbol or \"\")).lower()", "mutated": "    return (match.group(1) if match else (symbol or \"\"))", "note": "Dropped normalization: canonical_name no longer lowercases, so case-differing symbols are treated as distinct commodities."},
    {"id": "M07", "file": "APITool/store/market.py", "original": "    flags = [name for name in (\"Consumer\", \"Producer\", \"Rare\") if item.get(name)]", "mutated": "    flags = [name for name in (\"Consumer\", \"Producer\") if item.get(name)]", "note": "Swapped/dropped table of flag names: 'Rare' commodities silently lose their flag."},
    {"id": "M08", "file": "APITool/store/market.py", "original": "    if kind not in MARKET_KINDS:", "mutated": "    if kind in MARKET_KINDS:", "note": "Guard-clause negation dropped: project_one now bails out early for valid market kinds and proceeds (incorrectly) for invalid ones."},
    {"id": "M09", "file": "APITool/store/market.py", "original": "    if snapshot[\"market_id\"] is None:", "mutated": "    if False:", "note": "Guard clause removed: a snapshot with no market_id is no longer rejected and gets written with a null market_id."},
    {"id": "M10", "file": "APITool/store/market.py", "original": "    conn.execute(\"DELETE FROM market_item WHERE obs_id = ?\", (obs_id,))", "mutated": "    conn.execute(\"DELETE FROM market_item WHERE obs_id != ?\", (obs_id,))", "note": "Comparison flip == -> !=; deletes every other observation's market items instead of the stale rows for this obs_id."},
    {"id": "M11", "file": "APITool/cli.py", "original": "    if args.command and args.command != \"store\":", "mutated": "    if args.command and args.command == \"store\":", "note": "Comparison flip != -> ==; the first-run config write now fires only for the store verb instead of for every verb except it."},
    {"id": "M12", "file": "APITool/cli.py", "original": "    verb = args.store_command or \"verify\"", "mutated": "    verb = args.store_command and \"verify\"", "note": "Operator swap or -> and; when args.store_command is None/falsy, verb becomes None instead of defaulting to \"verify\"."},
    {"id": "M13", "file": "APITool/cli.py", "original": "        print(f\"No store at {path} -- nothing has been archived yet.\")", "mutated": "        pass", "note": "The missing-store message is dropped (the generator's is_file negation is not unique in cli.py; this is the same site's observable effect)."},
    {"id": "M14", "file": "APITool/cli.py", "original": "        return 0 if verb == \"verify\" else 2", "mutated": "        return 0 if verb != \"verify\" else 2", "note": "Comparison flip == -> !=; the missing-store exit code is now 0 for every verb except verify, inverted from intent."}
  ]
}
