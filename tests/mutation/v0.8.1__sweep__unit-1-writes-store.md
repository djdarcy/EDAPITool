# v0.8.1 sweep -- unit 1 of slice 4 (the writes ledger's storage)

## Environment

| | |
|---|---|
| Project | EDAPITool, uncommitted over `8c5772d` (v0.8.0) |
| Python | 3.12.0, SQLite 3.42.0, Windows 11 (10.0.26200) |
| Harness | `mutation_sweep.py` 0.1.0 (test-mutation skill) |
| Generation | mode 1 -- fresh subagent (Sonnet), the source inline, no tools, no tests |
| Date | 2026-09-25 |
| Spec / report | `v0.8.1__spec__unit-1-writes-store.json`; `v0.8.1__report__unit-1-writes-store.json` |

Scope: `APITool/store/writes.py` (new). Focused tests: `tests/test_store_writes.py`, `tests/test_store.py`. Danger assessment: every store is a `tmp_path` sqlite file through the conftest's `ED_CONFIG_DIR`; no spreadsheet, network or process is reachable.

## Kill table

| ID | Mutation | Verdict |
|---|---|---|
| M01 | `recorded()`'s guard `or` -> `and` | killed |
| M02 | None-normalisation inverted on read | killed |
| M03 | each chunk drops its 500th cell | killed |
| M04 | target and tab bindings swapped on read | killed |
| M05 | `record()`'s guard `or` -> `and` | killed |
| M06 | None-normalisation inverted on write | killed |
| M07 | `record()` returns False after success | killed |
| M08 | `StoreLedger.recorded` swaps target and tab | killed |
| M09 | `StoreLedger.record` swaps target and tab | killed |
| M10 | ownership detection flipped in `recorded()` | killed |
| M11 | `recorded()` closes a caller's connection | killed |

11/11. No survivors, so nothing to triage.

**Refinement round: not run.** The skill runs a second round after a 100% sweep when the mutants may have been too shallow. These were not: every one targets a behaviour the module's docstring promises, and M03, M10 and M11 hit edges (chunk boundaries, connection ownership) that a surface-level catalogue would not reach. The red-green audit before the sweep had already found and fixed the one test that could not fail (the chunk test, 1,500 cells under a 32,766 cap).

## Caveats

A kill proves the suite noticed *this* change, not that it tests the right thing -- and **a clean sweep is evidence about the mutations tried, not proof of suite completeness.**
