{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_store.py", "tests/test_stock_config.py"],
  "mutations": [
    {"id": "M09", "file": "APITool/cli.py", "original": "    if not argv or argv[0] == \"store\" or any(word in HELP_WORDS for word in argv):", "mutated": "    if not argv or argv[0] == \"serve\" or any(word in HELP_WORDS for word in argv):", "note": "Swapped verb constant: the first-run write now special-cases serve instead of store. Re-verified alone after the store test released the config-write guard."}
  ]
}
