# v0.8.1 sweep -- unit 5 of slice 4 (the report in the ledger's terms)

## Environment

| | |
|---|---|
| Project | EDAPITool, uncommitted over `8c5772d` (v0.8.0) |
| Python | 3.12.0, Windows 11 (10.0.26200) |
| Harness | `mutation_sweep.py` 0.1.0 (test-mutation skill) |
| Generation | mode 1 -- fresh subagent (Sonnet), the function inline, no tools, no tests; output rule stated first |
| Date | 2026-09-25 |
| Spec / report | `v0.8.1__spec__unit-5-report.json`; `v0.8.1__report__unit-5-report.json` |

Scope: `APITool/cli.py`'s `_report_plan`. Focused tests: `test_ledger_cli.py`, `test_goldens.py`, `test_marker_skip_occupied.py`, `test_location_opt_in.py`. The function prints; no mutant can reach a sheet, a file or the network.

## Kill table

| ID | Mutation | Verdict |
|---|---|---|
| M01 | held set built with `and` | **survived** |
| M02 | every marked row counted as held | killed |
| M03 | held qualifier inverted | killed |
| M04 | "(all)" for no marked rows | **survived** |
| M05 | refreshed and filled swapped | killed |
| M06 | empty lists printed too | killed |
| M07 | held count off by one | **survived** |
| M08 | `--force` hint with `and` | **survived** |
| M09 | tab prefix dropped | killed |
| M10 | forced and held swapped | killed |

6/10 in round 1. After two killing tests: 8/10, with two equivalents.

## Triage

| ID | Verdict | Reasoning | Killing test |
|---|---|---|---|
| M01 | equivalent | the writer always sets `skipped` equal to `held` | recorded in `equivalents.md` |
| M04 | killable | no test printed a plan with nothing marked | `test_a_plan_with_nothing_marked_says_none` |
| M07 | killable | the marked-row test checked the rows but not the count | `test_a_marked_row_whose_cell_is_held_says_so_on_both_paths`, tightened to the count |
| M08 | equivalent | the same equal lists | recorded in `equivalents.md` |

Both killing tests were shown red under their mutants and green restored.

## Caveats

A kill proves the suite noticed *this* change, not that it tests the right thing -- and **a clean sweep is evidence about the mutations tried, not proof of suite completeness.**
