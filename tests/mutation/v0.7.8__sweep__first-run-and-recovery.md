# v0.7.8 sweep -- the config cluster (units 1 and 2 of slice 1)

## Environment

| | |
|---|---|
| Project | EDAPITool, uncommitted diff over `6ef3a7e` (v0.7.7) |
| Python | 3.12.0, Windows 11 (10.0.26200) |
| Harness | `mutation_sweep.py` (test-mutation skill) |
| Generation | mode 1 -- fresh subagent, source inline, no tools, no tests |
| Date | 2026-09-24 |
| Spec / report | `v0.7.8__spec__first-run-and-recovery.json` + `-rerun.json`; `v0.7.8__report__first-run-and-recovery.json` + `-rerun.json` (first named `config-cluster`, which `.gitignore`'s `*config*.json` swallowed; renamed before the commit) |

Scope: the lines added to `APITool/settings.py` (header split, parse, once-per-path report, recovery, `.bak`), `APITool/stockconfig.py` (new), and the first-run hook in `APITool/cli.py`. One sweep for two units, because both live in the same files; the docs unit (3) has no code. Focused tests: `test_settings.py`, `test_stock_config.py`, `test_plugin_loader.py`, `test_config_surface.py`.

## Kill table

| ID | File | Mutation | First sweep | Re-run |
|---|---|---|---|---|
| M01 | settings.py | a blank line ends the header | killed | |
| M02 | settings.py | off-by-one: the opening brace joins the header | killed | |
| M03 | settings.py | a non-dict JSON body returned as-is | **survived** | killed |
| M04 | settings.py | once-per-path report inverted | killed | |
| M05 | settings.py | corrupt body reads `{}` silently (the old behaviour) | killed | |
| M06 | settings.py | previous file never "good", so no `.bak` | killed | |
| M07 | settings.py | a corrupt previous file overwrites the good `.bak` | killed | |
| M08 | settings.py | `save()` drops the header | killed | |
| M09 | stockconfig.py | shipped plugins skipped, user ones taken | killed | |
| M10 | stockconfig.py | the `ED_NO_STOCK_CONFIG` guard removed | killed | |
| M11 | stockconfig.py | `exists()` inverted: overwrite, never create | killed | |
| M12 | cli.py | `main()` never prints the written-file line | **harness-error** | killed |

## Anomaly

M12's first form replaced `print(wrote)` with a bare comment, leaving `if wrote:` with an empty block -- a `SyntaxError`, so every test errored and the harness rightly refused the verdict. Re-run with `pass  # mutant`: killed by `test_the_first_command_writes_the_stock_file_and_says_so`.

## Triage

| ID | Verdict | Reasoning | Killing test |
|---|---|---|---|
| M03 | killable | the `isinstance(data, dict)` guard predates this work and nothing ever planted a list or a string body | `tests/test_settings.py::test_a_body_that_is_valid_json_but_not_an_object_reads_as_nothing` -- red under M03, green on the real code |

No refinement round beyond the two-mutant re-run: the survivor was closed and the anomaly was the spec's, not the suite's.

## Caveats

A kill proves the suite noticed *this* change, not that it tests the right thing. **A clean sweep is evidence about the mutations tried, not proof of suite completeness.**
