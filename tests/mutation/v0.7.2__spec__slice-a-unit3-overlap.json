{
  "test_command": ["python", "-m", "pytest", "-q", "-p", "no:cacheprovider", "tests/test_plugin_loader.py", "tests/test_sheets.py"],
  "mutations": [
    {"id": "m1-a1-overlaps-and-or", "file": "APITool/sheets/a1.py", "original": "        return (\n            self._meets(self.first_col, self.last_col, other.first_col, other.last_col)\n            and self._meets(self.first_row, self.last_row, other.first_row, other.last_row)\n        )", "mutated": "        return (\n            self._meets(self.first_col, self.last_col, other.first_col, other.last_col)\n            or self._meets(self.first_row, self.last_row, other.first_row, other.last_row)\n        )", "note": "and->or: sharing a column OR a row counts as overlap."},
    {"id": "m2-a1-meets-truthiness", "file": "APITool/sheets/a1.py", "original": "        # a ends before b starts?\n        if a_hi is not None and b_lo is not None and a_hi < b_lo:\n            return False", "mutated": "        # a ends before b starts?\n        if a_hi and b_lo and a_hi < b_lo:\n            return False", "note": "is-not-None becomes truthiness; index 0 behaves as unbounded."},
    {"id": "m3-a1-meets-comparison-flip", "file": "APITool/sheets/a1.py", "original": "        # b ends before a starts?\n        if b_hi is not None and a_lo is not None and b_hi < a_lo:\n            return False", "mutated": "        # b ends before a starts?\n        if b_hi is not None and a_lo is not None and b_hi > a_lo:\n            return False", "note": "< -> > on the second axis test."},
    {"id": "m4-loader-overlapping-wrong-key", "file": "APITool/loader.py", "original": "        for tab in sorted(set(a) & set(b)):\n            for left in a[tab]:\n                for right in b[tab]:", "mutated": "        for tab in sorted(set(a) & set(b)):\n            for left in a[tab]:\n                for right in a[tab]:", "note": "Compares a's ranges to themselves."},
    {"id": "m5-loader-severity-ignore-constant", "file": "APITool/loader.py", "original": "SEVERITY_WARN = \"warn\"\nSEVERITY_IGNORE = \"ignore\"\nSEVERITIES = (SEVERITY_ERROR, SEVERITY_WARN, SEVERITY_IGNORE)", "mutated": "SEVERITY_WARN = \"warn\"\nSEVERITY_IGNORE = \"ignored\"\nSEVERITIES = (SEVERITY_ERROR, SEVERITY_WARN, SEVERITY_IGNORE)", "note": "The ignore constant no longer spells 'ignore'."},
    {"id": "m6-loader-severity-guard-flip", "file": "APITool/loader.py", "original": "    if severity not in SEVERITIES:\n        raise ValueError(f\"severity must be one of {', '.join(SEVERITIES)}, got {severity!r}\")", "mutated": "    if severity in SEVERITIES:\n        raise ValueError(f\"severity must be one of {', '.join(SEVERITIES)}, got {severity!r}\")", "note": "Validation inverted."},
    {"id": "m8-loader-conflict-raise-and-or", "file": "APITool/loader.py", "original": "        found_conflicts = conflicts(result.loaded)\n        if found_conflicts and severity == SEVERITY_ERROR:", "mutated": "        found_conflicts = conflicts(result.loaded)\n        if found_conflicts or severity == SEVERITY_ERROR:", "note": "and->or: raises on the happy path under error, and under warn with conflicts."},
    {"id": "m9-loader-precedence-reverse", "file": "APITool/loader.py", "original": "        rank = {name: i for i, name in enumerate(_unique(precedence))}\n        result.loaded.sort(key=lambda e: rank.get(e.name, len(rank)))", "mutated": "        rank = {name: i for i, name in enumerate(_unique(precedence))}\n        result.loaded.sort(key=lambda e: rank.get(e.name, len(rank)), reverse=True)", "note": "Precedence reversed: last-listed wins."},
    {"id": "m10-settlement-writes-empty", "file": "APITool/plugins/settlement/__init__.py", "original": "def writes() -> dict[str, list[str]]:\n    \"\"\"What this plugin declares it writes, as shipped. One definition: the layout's.\"\"\"\n    return SheetLayout().writes()", "mutated": "def writes() -> dict[str, list[str]]:\n    \"\"\"What this plugin declares it writes, as shipped. One definition: the layout's.\"\"\"\n    return {}", "note": "The shipped plugin declares nothing at load."}
  ]
}
